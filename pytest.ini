[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = 
    --verbose
    --capture=no
    --strict-markers
    --maxfail=1
markers =
    smoke: Smoke tests
    regression: Regression tests
    api: API tests
    ui: UI tests
